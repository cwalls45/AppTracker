import { Grid } from "@mui/material";

const SignUpForm = () => {

    return (
        <Grid container sx={{
            height: '100vh',
            width: 'auto'
        }}>
            SignUpForm
        </Grid >
    );
}

export default SignUpForm;